global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: "clean-api"
    metrics_path: /metrics
    bearer_token_file: /run/secrets/prom_metrics_token
    static_configs:
      - targets: ["api:8000"]
