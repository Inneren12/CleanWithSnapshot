[pytest]
markers =
    smoke: marks tests as high level smoke flows running against Postgres
    sanity: quick dependency sanity checks for CI
    postgres: tests that require PostgreSQL
    migrations: migration invariants and schema checks
    anyio: async tests running under AnyIO
    asyncio: async tests using asyncio event loop
    e2e: end-to-end tests
    slow: slow-running tests
